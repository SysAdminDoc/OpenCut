services:
  opencut-server:
    build: .
    ports:
      - "5679:5679"
      # Default Docker runtime publishes the HTTP API only. Optional
      # WebSocket/MCP sidecars require an explicit custom service/profile.
    volumes:
      # Persist models, settings, job history across container rebuilds
      # Match the Dockerfile's non-root ``opencut`` user home; mounting to
      # ``/root/.opencut`` writes to the wrong path (or fails outright)
      # because the runtime user is ``opencut`` (uid != 0), not root.
      - opencut-data:/home/opencut/.opencut
    environment:
      - OPENCUT_BUNDLED=false
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5679/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # GPU-enabled variant - use: docker compose --profile gpu up opencut-server-gpu
  opencut-server-gpu:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5679:5679"
      # Default Docker runtime publishes the HTTP API only. Optional
      # WebSocket/MCP sidecars require an explicit custom service/profile.
    volumes:
      # Match the Dockerfile's non-root ``opencut`` user home; mounting to
      # ``/root/.opencut`` writes to the wrong path (or fails outright)
      # because the runtime user is ``opencut`` (uid != 0), not root.
      - opencut-data:/home/opencut/.opencut
    environment:
      - OPENCUT_BUNDLED=false
      - PYTHONUNBUFFERED=1
      - NVIDIA_VISIBLE_DEVICES=all
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5679/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    profiles:
      - gpu

volumes:
  opencut-data:
