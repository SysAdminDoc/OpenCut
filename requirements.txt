# OpenCut v1.33.1 Requirements
# Install: pip install -r requirements.txt
# For GPU: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# === CORE (required) ===
# click CVE-2026-7246 / PYSEC-2026-2132 (command injection in click.edit) fixed 8.3.3
click>=8.3.3,<9
rich>=13.0,<14
flask>=3.1.3,<4
flask-cors>=6.0,<7
waitress>=3.0.1,<4
python-json-logger>=2.0,<3
psutil>=5.9
keyring>=25.7,<26
# RA-23 — Werkzeug/Jinja2 ship with flask; pin the security floor (Werkzeug
# CVE-2026-21860/CVE-2025-66221 fixed 3.1.5, CVE-2026-27199 fixed 3.1.6;
# Jinja2 CVE-2025-27516 fixed 3.1.6).
Werkzeug>=3.1.6
Jinja2>=3.1.6

# === STANDARD (recommended) ===
faster-whisper>=1.1,<2
opencv-python-headless>=4.13,<5
Pillow>=12.2,<13
numpy>=1.24
librosa>=0.10,<1
# pydub retired in F123; OpenCut does not import it and keeps an optional
# audioop-lts compatibility shim for downstream plugins that install pydub.
noisereduce>=3.0,<4
scenedetect[opencv]>=0.6,<1
# RA-23 — requests/urllib3 enter via faster-whisper's huggingface-hub; floor
# them (urllib3 CVE-2026-21441 fixed 2.6.3, requests CVE-2026-25645 fixed 2.33.0).
requests>=2.33.0
urllib3>=2.6.3

# === AUDIO (optional) ===
# demucs>=4.0              # Stem separation (~1.5GB with models)
# pedalboard>=0.9          # Pro audio FX chain
# edge-tts>=6.1            # Text-to-speech (requires internet)

# === AI/ML (optional, GPU recommended) ===
# realesrgan>=0.3          # AI upscaling (~67MB model)
# rembg>=2.0               # Background removal (~170MB model)
# gfpgan>=1.3              # Face enhancement (~330MB model)
# insightface>=0.7         # Face detection/swap
# onnxruntime-gpu>=1.25    # GPU inference for InsightFace
# audiocraft>=1.3          # MusicGen AI music (Python 3.11; Torch 2.1 stack)

# === ADVANCED (optional) ===
# whisperx>=3.8.5          # Word-level timestamps + alignment
# pyannote.audio>=4.0      # Speaker diarization (needs HuggingFace token)

# === POST-PRODUCTION FEATURES (optional) ===
# Color matching & auto-zoom use opencv-python-headless + numpy (already listed above)

# Loudness matching uses FFmpeg subprocess (external tool, not pip)
# Install FFmpeg from https://ffmpeg.org/download.html

# Footage search uses stdlib json only (no extra deps)

# LLM integration (for chapters, NLP commands) — optional
# ollama: run locally via https://ollama.ai
# openai>=1.0.0            # For OpenAI provider
# anthropic>=0.20.0        # For Anthropic provider
