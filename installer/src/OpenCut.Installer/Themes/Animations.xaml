<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Easing Functions -->
    <CubicEase x:Key="EaseOut" EasingMode="EaseOut" />
    <CubicEase x:Key="EaseInOut" EasingMode="EaseInOut" />
    <BackEase x:Key="SpringOut" EasingMode="EaseOut" Amplitude="0.3" />
    <ElasticEase x:Key="BounceOut" EasingMode="EaseOut" Oscillations="1" Springiness="5" />

    <!-- Fade In -->
    <Storyboard x:Key="FadeIn">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0" To="1" Duration="0:0:0.4"
                         EasingFunction="{StaticResource EaseOut}" />
    </Storyboard>

    <!-- Slide Up + Fade In -->
    <Storyboard x:Key="SlideUpFadeIn">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0" To="1" Duration="0:0:0.5"
                         EasingFunction="{StaticResource EaseOut}" />
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                         From="30" To="0" Duration="0:0:0.5"
                         EasingFunction="{StaticResource SpringOut}" />
    </Storyboard>

    <!-- Progress Shimmer -->
    <Storyboard x:Key="ProgressShimmer" RepeatBehavior="Forever">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                         From="-200" To="600" Duration="0:0:1.5"
                         EasingFunction="{StaticResource EaseInOut}" />
    </Storyboard>

    <!-- Checkmark Draw Animation -->
    <Storyboard x:Key="CheckmarkDraw">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                         From="0" To="1" Duration="0:0:0.4"
                         BeginTime="0:0:0.1"
                         EasingFunction="{StaticResource BounceOut}" />
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                         From="0" To="1" Duration="0:0:0.4"
                         BeginTime="0:0:0.1"
                         EasingFunction="{StaticResource BounceOut}" />
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0" To="1" Duration="0:0:0.3"
                         EasingFunction="{StaticResource EaseOut}" />
    </Storyboard>

    <!-- Pulse glow for active step indicator -->
    <Storyboard x:Key="PulseGlow" RepeatBehavior="Forever" AutoReverse="True">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0.6" To="1.0" Duration="0:0:0.8"
                         EasingFunction="{StaticResource EaseInOut}" />
    </Storyboard>

</ResourceDictionary>
