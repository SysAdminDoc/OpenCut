# OpenCut — Test Coverage Gaps (Pass 2)

**Audit date:** 2026-05-17
**Source:** `tests/` directory (131 files), CI workflow (`coverage --fail-under=50`), CLAUDE.md test list, repo grep.

---

## 1. Numbers

| Metric | Value |
|---|---|
| Test files (`tests/test_*.py`) | **126** + `conftest.py` + `fuzz/` + `jsx_mock.js` |
| Top-level `def test_*` functions | **297** (parameterised + classes amplify the real count) |
| README claim | "5,742+ tests across 77 test files" |
| CHANGELOG claim | "7,551 tests" |
| ROADMAP claim | "7,600+ tests" |
| CI coverage floor | **50%** (`--cov-fail-under=50`) |
| Skip / skipif markers | **36 occurrences** across 10 files (all gated on `ffmpeg` PATH or optional `flask_cors`) |

The marketing test count and the file count diverge; pytest's actual collected count is somewhere in the thousands once parameterised tests + class-based tests fan out. The **CI coverage floor of 50% is the real ground truth**, and it's a generous floor.

---

## 2. What's well tested

From the test-file inventory and CLAUDE.md narrative:

- **Route smoke tests** (`tests/test_route_smoke.py` ~950 lines, 107+ tests across all 11 original blueprints)
- **Core module unit tests** (`test_core_modules.py`, `test_core_modules_batch2.py`, `test_new_modules.py` — 135+ tests across 28 modules)
- **Job system** (`test_job_store.py`, `test_workflow.py`, `test_batch_executor.py`, `test_batch_parallel.py`)
- **Plugins** (`test_plugins.py`, `test_clip_notes_plugin.py`)
- **Hardening** (`test_hardening.py`, `test_boolean_coercion.py`, `test_dev_scripting.py`, `test_workflow_dev.py`, `test_workflow_auto.py`)
- **Streaming, footage index, config** (`test_streaming.py`, `test_footage_index_db.py`, `test_config_and_userdata.py`)
- **Integration** (`test_integration.py`, `test_integration_ffmpeg.py`, `test_integration_whisper.py` — both skip without ffmpeg in PATH)
- **JSX mock** (`tests/jsx_mock.js`, 38 functions, 35 assertions under Node.js)
- **Per-wave / per-feature**: `test_ai_eval_harness.py`, `test_c2pa_sidecar.py`, `test_capability_profile.py`, `test_caption_qc.py`, `test_marker_import.py`, `test_marker_metadata.py`, `test_fcp_transitions.py`, `test_local_auth.py`, `test_model_cards.py`, `test_ocio_validate.py`, `test_plugin_manifest.py`, `test_project_health.py`, `test_review_bundle.py`, `test_route_manifest.py`, `test_seed_github_issues.py`, `test_release_smoke.py`, `test_bootstrap_check.py`, `test_crash_packet.py`, `test_job_diagnostics.py`, `test_license_gate.py`, `test_roadmap_lint.py`, `test_windows_arm64_doc.py`, `test_feature_state_panel_helper.py` — basically every F-number Now-tier item has its own test file. **This is exemplary** — it's why the *Now* tier shipped so quickly.

---

## 3. Where coverage is thin

Inferred from the lack of dedicated test files (or thin coverage of large modules):

### 3.1 Wave-letter Tier 2/3 stubs

The Wave K/H Tier 2 stubs (FlashVSR, ROSE, Sammie, OmniVoice, ReEzSynth, VidMuse, GPT-SoVITS, Amphion, Vevo2, CosyVoice2, EchoMimic, TokenFlow, Cutie, DEVA, SEA-RAFT, DiffBIR, Gyroflow, motion deblur, Depth Pro, DepthFlow, AudioGen, Open-Sora, LTX-2, audio-reactive FX, CineFocus) and Tier 3 stubs (video_agent, gen_video_cloud, lipsync_advanced, dub_pipeline, trailer_gen, screenplay_parser, face_age_transform, slate_id, outpaint_video, gen_video_wan_vace, highlights_sports) — most have **only stub-return tests** (e.g. asserting 503 / 501 + install hint), not real-input integration tests because the backends aren't installed.

**Gap:** when these stubs eventually fill in (per F148-F156, F162-F174), each will need its own integration test with sample media. The F176 eval dataset bundle (Now-tier) should be paired with these so every wired-up backend has a smoke test against a known good sample.

### 3.2 OpenAPI surface tests

`tests/test_route_manifest.py` pins the manifest contract. Pass 13 closed **F208 — OpenAPI spec validity test** by adding `tests/test_openapi_contract.py` to release smoke. The new gate asserts the OpenAPI 3.0.3 spec generated by `opencut/openapi.py` covers every non-static Flask route/method, emits OpenAPI `{param}` syntax with path parameter metadata, has unique operation IDs, and includes 200 plus mutating-method 400/403 response shapes.

### 3.3 MCP tool coverage

Pass 14 closed **F209 — MCP tool ↔ route consistency test**. `tests/test_mcp_server.py` now verifies every tool in `MCP_TOOLS` has a route mapping, every mapped route exists in the live Flask app, and dynamic dispatch routes for Brand Kit, semantic search, music generation, arbitrary style transfer, and job status remain valid.

### 3.4 Frontend (CEP main.js + UXP main.js)

The CEP `main.js` is ~7,730 lines of vanilla JS. The UXP `main.js` is ~1,500 lines of ES module. The only JS test is `tests/jsx_mock.js` covering the **ExtendScript host (`host/index.jsx`)** — not `main.js`. There is no JS unit-test suite for the panel-side controllers.

Adding Jest / Vitest tests for `main.js` is heavy lift (Bolt CEP doesn't have it either). But discrete utilities (`esc()`, `escPath()`, command-palette indexer, lazy DOM proxy) could be tested in isolation. **F210 — Vitest unit tests for CEP/UXP utility functions** (M effort).

### 3.5 Cross-platform launchers

`OpenCut-Server.bat`, `.vbs`, `.command`, `.sh` — these are not under test. A simple "launcher exits 0 with `--check` flag" smoke would catch path-quoting bugs on each platform. **F211 — launcher script smoke tests in CI** (small).

### 3.6 Installer

The WPF .NET 9 installer (~2,326 lines of C#) has no automated tests. The Inno Setup output is functional-tested only by manual install. The PyInstaller spec is import-smoke-tested only.

This is the **largest single coverage gap** in the project. **F212 — WPF installer test suite** (XL — would need xUnit project + headless install in a Windows runner). **F213 — Inno Setup install/uninstall smoke** (M — install to temp dir, check shortcut creation, uninstall, check cleanup).

### 3.7 Performance / regression benchmarks

The F128 FFmpeg filter regression suite is *Now* tier but not yet shipped. Once it lands, it should also cover:
- **Whisper / faster-whisper / WhisperX** wall-clock per second of source audio on a known-corpus
- **Real-ESRGAN / FlashVSR / SeedVR** wall-clock per frame on a known sample
- **Compose / declarative_compose** wall-clock per 30s clip
- **TTS** wall-clock per second of synthesised audio per backend

These guard against silent perf regressions that the 50% coverage floor cannot catch. **F214 — extend F128 to cover ML + TTS perf benchmarks** (M).

### 3.8 Fuzz coverage

Pass 38 closed F215: `tests/fuzz/test_parser_fuzz.py` now covers SRT time, SRT file, CUBE LUT, voice grammar, event-moment spike parsers, `validate_path` / `validate_filepath`, OTIO JSON/adapter parsing, FCPXML parsing, marker import, C2PA sidecar verification, plugin manifest validation, webhook HMAC signature verification, and `safe_pip_install` package-spec validation (13 targets). Atheris-gated, opt-in via `RUN_FUZZ=1`, with `tests/test_fuzz_harness_targets.py` pinning the inventory in normal CI.

Previously missing fuzz targets that were **high value** for an HTTP server handling user file paths:
- `validate_path` / `validate_filepath` (the F069/F070 hardening surface — explicitly noted in v4.3 audit)
- OTIO XML/JSON parsing (F104 / F126 OTIO-Plugins-related)
- FCP XML transition writer (F104)
- Marker import (CSV / Premiere CSV / EDL) (F102)
- C2PA sidecar parser (F110)
- Plugin manifest validator (F116)
- Webhook signature verifier
- safe_pip_install URL/path checker

**[x] F215 — extend fuzz harness with 8 additional targets** (M). Closed in Pass 38.

### 3.9 Concurrency / race conditions

Pass 39 closed **F216 — concurrent job-cancellation race test** (M). `_cancel_job()` now terminates registered child processes, closes parent pipe handles so blocked FFmpeg progress reads wake up, and `tests/test_job_cancellation_race.py` verifies cancellation during an active `_run_ffmpeg_with_progress()` run cleans up the process registry.

### 3.10 UXP panel ↔ backend integration

There's `tests/uxp/` (not seen in listing) or equivalent? No — the test list shows no UXP-specific test file. The UXP panel is tested only via the JSX mock harness (`tests/jsx_mock.js`) and manual Premiere testing. A headless UXP smoke (e.g. via `puppeteer` against a launched UXP debug instance) is **too high effort** for current value, but a contract test for the UXP `BackendClient` HTTP shape is achievable. **F217 — UXP backend-client contract test** (S).

### 3.11 Cross-blueprint dependency tests

Pass 15 closed **F218 — import-order stability test for blueprint registration**. `tests/test_route_collisions.py` now pins the exact `get_core_blueprints()` order, asserts the live app appends `motion_design_api` last for the legacy `/api/motion/*` surface, and is included in release smoke.

### 3.12 SBOM regression

Pass 16 closed **F219 — SBOM completeness test**. `scripts/sbom.py` now emits unique declared Python dependency components, model-card components for all 47 `opencut.model_cards.CARDS` entries, and a non-empty CycloneDX `dependencies` graph. `tests/test_sbom_completeness.py` is wired into release smoke and asserts declared dependency coverage, generated model-card parity, unique `bom-ref` values, and dependency graph coverage.

---

## 4. Coverage floor uplift plan

Current: `--cov-fail-under=50`. CHANGELOG / ROADMAP claim 7,551+ tests, which would imply much higher actual coverage. A pragmatic uplift sequence:

1. **F205** (already proposed in INSTALLER_AUDIT.md) — measure actual coverage today, then floor at `actual - 5%` to absorb noise. Pass 23's interrupted 36m46s local run produced partial ignored JSON reporting 52.12% coverage, but pytest did not complete, so the number is not usable for this decision.
2. After F176 (eval dataset bundle) + F215 (fuzz extend) + F218 (import-order test) ship: re-measure and re-floor.
3. Target: 75-80% actual coverage with a 70% floor by v1.40.

This is a single F number: **F205**, already on the Now tier.

---

## 5. Recommended new F-numbers (from this Pass-2 test audit)

| F# | Title | Priority | Effort |
|---|---|---|---|
| F208 | OpenAPI spec validity test (per-endpoint 200/400/403) | Done in Pass 13 | S |
| F209 | MCP tool ↔ route consistency test | Done in Pass 14 | S |
| F210 | Vitest unit tests for CEP/UXP utility functions | Later | M |
| F211 | Cross-platform launcher script smoke tests in CI | Next | S |
| F212 | WPF installer test suite (xUnit + headless install) | Later | XL |
| [x] F213 | Inno Setup install/uninstall smoke in CI | Closed Pass 37 | M |
| F214 | Extend F128 with ML + TTS perf benchmarks | Next | M |
| [x] F215 | Extend fuzz harness with 8 additional targets | Closed Pass 38 | M |
| [x] F216 | Concurrent job-cancellation race test | Closed Pass 39 | M |
| F217 | UXP backend-client contract test | Next | S |
| F218 | Import-order stability test for blueprint registration | Done in Pass 15 | S |
| F219 | SBOM completeness test | Done in Pass 16 | S |
